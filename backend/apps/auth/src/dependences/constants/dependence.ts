export enum DependenceStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum DependenceType {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  MIXT = 'MIXT',
}

export enum DependenceTributaryType {
  CUIT = 'CUIT',
}
