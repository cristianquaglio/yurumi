import { Outlet } from 'react-router-dom';

const ListDependencesPage = () => {
    return (
        <>
            <h1>ListDependencesPage</h1>
            <Outlet />
        </>
    );
};

export default ListDependencesPage;
